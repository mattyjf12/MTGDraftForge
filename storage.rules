rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Avatar images: anyone can read, only authenticated users can write their own
    match /avatars/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
